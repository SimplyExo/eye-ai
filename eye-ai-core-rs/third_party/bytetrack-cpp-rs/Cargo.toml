[package]
name = "bytetrack-cpp-rs"
version = "0.1.1"
edition = "2024"

[features]
native-test-api = []

[build-dependencies]
cmake = "0.1.54"

[dev-dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
