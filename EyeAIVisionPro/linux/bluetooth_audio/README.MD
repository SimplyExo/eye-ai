# Bluetooth Audio
Installer script to simply install a bluetooth audio sink using a2dp. In the future we will use the hfp protocol to support recording audio.

## Installation
Just run ```install.sh```. It will clone the repo [rpi-audio-receiver](https://github.com/nicokaiser/rpi-audio-receiver.git) and run the installation.